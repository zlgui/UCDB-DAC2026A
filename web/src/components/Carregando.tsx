import { cores } from "../utils/tema.util";

export function Carregando() {
  return (
    <div style={{ padding: "3rem", textAlign: "center", color: cores.textoSecundario }}>
      <p>Carregando dados...</p>
    </div>
  );
}
